{
  "name": "@atlitos/ui-native",
  "version": "0.1.0",
  "private": true,
  "description": "Atlitos mobile component library (Expo/React Native). Ships a placeholder ThemedText today; P1 rebuilds the full 42-component library here on react-native-reusables, all reading @atlitos/theme, none reading a raw hex or px value.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "dev": "echo \"@atlitos/ui-native has no standalone dev server, it is consumed by apps/mobile\"",
    "build": "echo \"@atlitos/ui-native ships as source, no build step until an app bundler needs one\"",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@atlitos/theme": "workspace:*"
  },
  "peerDependencies": {
    "react": ">=18.2.0",
    "react-native": ">=0.74.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "react": "^18.3.1",
    "react-native": "^0.76.3",
    "typescript": "^5.6.3"
  }
}
