# Resume state for ATLITOS v2

Read by `~/.claude/bin/claude-resume` after a usage-limit reset, and by any
fresh Claude session picking this project back up. Keep the PROMPT line
current: it is the single instruction a cold session gets.

PROMPT: Resume ATLITOS v2 at Phase 6 (Empower / Atlitos Life portal) and continue autonomously through P7, P8 to a finished end-to-end product. Read docs/phases/PHASE-6-STATUS.md (once the planner writes it) and docs/phases/PHASE-5-STATUS.md handoff notes, plus this file's Current position, then continue the workflow in docs/agents/workflows.md. Do not wake the founder for anything under Founder decisions below. Commit after each milestone and keep this file's Current position updated.

FINISH LINE = TESTFLIGHT (founder decision 2026-07-22): "ready end to end" means all the way to TestFlight, not just web-proven through P8. So after P8 come P9 (native verification pass: run the real iOS/Android app, burn down native debt) and P10 (TestFlight ship: signed build + App Store Connect + Apple review). HARD DEP: founder's Apple Developer account ($99/yr, 24-48h to activate) for P10, cannot be scripted; flagged to founder to start enrollment early. No automated test suite is planned unless separately scoped. See docs/PLAN.md "Post-P8 ship stages".

P0-P5 CLOSED. P5 Clutch APPROVED cycle 1 (2026-07-22, commit 17b40f5): scripted-real pipeline proven end to end (real MP4 in via stream-upload-url, played back 200 with 9409 matching bytes out of get-clip-playback-url), takedown teeth, reconcile arm, state machine, RLS all independently re-verified. The founder's full-autonomy directive (below) let the scripted proof stand in for the native upload; only the device-only sliver (camera capture, react-native-video autoplay, haptics, gallery picker) is carried to a native pass.

WORKTREE STALE-BASE FIX (learned the hard way in P5): isolation:"worktree" agents get created from origin/main, which is STALE because main has not been pushed. Track C built 3 phases stale and needed a manual merge. FIX for all remaining phases: every worktree builder must run `git merge main --no-edit` as step 0 to pull current work before building; put that in every builder prompt. (Alternative not taken: push local main to origin, or set worktree.baseRef=head.)

FULL-AUTONOMY DIRECTIVE (founder, 2026-07-22): "Go autonomous till the product is ready end to end. Complete loops, complete tests, till the final product or I interrupt. The founder will NOT test." So: (1) drive every phase gate to APPROVE without waiting on founder input; (2) where a gate normally needs a founder finger (P5 "upload clip from device", any native tap), REPLACE it with a scripted-but-real proof through the actual deployed backend, e.g. push a real MP4 through the live stream-upload-url edge function and drive the pipeline uploading->processing->ready->moderation->published->feed, and document the on-device-camera-UI sliver as the ONLY native-deferred piece rather than stalling; (3) verify everything with SQL or a real artifact, never assert (anti-hallucination); (4) keep subagent concurrency LOW (2 tracks at a time) after the 2026-07-21/22 API instability that stalled a 4-wide fan-out; commit incrementally; resume a stalled/API-killed agent from transcript, and if it stalls twice, wait rather than thrash.

## Current position (2026-07-20, P4 build in flight)

P0 through P3 are CLOSED. P3 was APPROVED at cycle 2 (commit d05835d) after the
approver independently verified the money: session 99384050 rated against a
captured intent, ledger group 3ef7ac3f balanced 1000 / 990 / 10, refund
rfnd_TFhrCu5zWLRuzd processed, six ledger groups all balanced with no double
accrual, coach wallet 990 agreeing across three independent readings.

P4 planned as commit 2e5abcb, founder decisions recorded in 2655ac8.

- Track A DONE and merged to main: migrations 0031 to 0037, commerce schema,
  RLS, and the stock_reservations design (15 min TTL, three exits). Oversell
  guard proven under genuinely concurrent connections. Writing that probe found
  a real bug, a `count(*) ... FOR UPDATE` Postgres rejects.
- Tracks B (payments, opus), C (mobile shop, sonnet), D (admin, sonnet),
  E (fixtures, haiku) launched in parallel on their own p4/track-* branches.
- Track F verification runs AFTER those merge. Then integrator, then the biased
  approver.

Read `product_variant_availability`, never `product_variants.stock`, for any
number shown to a shopper. Reservation RPCs and order_transition are
service_role only. Jira project key is `AT`, not ATL, and the board has NO
In Review status, so tickets stay In Progress with an implementation comment.

## Superseded: P3 cycle-2 punch list, all closed

GOAL, founder stated 2026-07-20: all nine phases complete and the app working
end to end (browse and book courts, coaching, chat, commerce, watch other
people's videos, donations, learn) BEFORE showing an investor. Not a partial
demo. Plan accordingly: no phase gets skipped, but no phase gets gold-plated
either.

P0, P1, P2 CLOSED. P3 build complete, 30 stories (AT-35 to AT-64), REJECTED at
cycle 1 on unwitnessed money, punch list now nearly closed:
- Items 1 and 2 DONE: two real native payments produced the first session
  earnings accrual (990 coach, 10 fee) and the first FR-35 refund (1000,
  processed, idempotent). Coach wallet reads 990 from a real ledger group.
  Evidence docs/phases/evidence/p3-native/MONEY-LIFECYCLE.md.
- Item 5 DONE: advisories written into PHASE-3-STATUS.md, boxes reconciled,
  PAYMENTS.md corrected.
- Items 3 and 4 IN FLIGHT: re-drive browse-to-book post-AT-63 and capture dark
  mode, landing in docs/phases/evidence/p3-web-cycle2/.

NEXT, in order:
1. When items 3 and 4 land, re-run the biased approver for P3 cycle 2. It said
   it expects to approve on cycle 2. Judge against the AMENDED gate.
2. On APPROVE: phase-close agent per docs/agents/workflows.md section 5, then
   the P4 planner.
3. P4 Commerce, P5 Clutch video, P6 Empower and Life portal, P7 Learn and XP,
   P8 hardening. P4 and P6 touch money and will run slower. P5 does NOT need a
   Cloudflare account: VIDEO.md line 153 records the founder declining the
   $5/mo Stream block at P0, so v1 Clutch ships on a SUPABASE STORAGE adapter
   behind the same stream-upload-url / stream-webhook contracts, progressive
   MP4 not HLS, with clips columns kept Stream-shaped so the swap is later a
   config + one-adapter change, not a migration. Do not re-request Cloudflare.
   P8 owes the deferred Route transfer, native screen coverage, the checkout
   failure path, and the accumulated RLS warns.

WORKING METHOD THAT IS EARNING ITS KEEP, keep doing it: every "prove it, do not
assert it" story this build has found something real. P3 alone: a money bypass,
a false real-time claim, a dead coach-discovery path, and copy that told
athletes the opposite of what the API did. Typechecks and green builds caught
none of them. Delegate heavily to subagents to protect main-thread context, and
make each one VERIFY rather than report.

## Founder decisions already made, do not re-ask

- FR-19 amended 2026-07-19: athlete may cancel an unanswered `requested`
  session, automatic full refund, no fee retained. Implemented as AT-60.
- Sessions carve the platform fee OUT of price (total = price); courts add it
  on top. Intentional, keep it, make the coach UI say what they receive.
- Cycle 3 of the P2 gate ran under founder pre-authorization.
- P4 roundup, decided 2026-07-20: the cart total rounds UP TO THE NEXT MULTIPLE
  OF 10, derived server side after delivery and GST, multiple held in fee_config.
  This overrode the plan's flat-figure assumption. Zero-roundup carts must write
  NO donation ledger leg and must suppress the row.
- P4 delivery, decided 2026-07-20: flat per order, from fee_config, added before
  the roundup is derived.

## Blocked on the founder, do not attempt to work around

- Razorpay Route is NOT enabled on the test merchant account. Live probe:
  POST /v2/accounts returns 400 "Route feature not enabled for the merchant".
  Blocks AT-43 and the P3 gate's visible-transfer clause. If it needs full KYC
  he does not want yet, the agreed fallback is to amend the P3 gate rather than
  stall the remaining phases.
- Delete the `tmp-seed-demo-users` edge function (needs dashboard access).
- iOS simulator taps still need either his finger or Accessibility plus Screen
  Recording granted AND the terminal restarted.


## Corrected: the gen-tokens typecheck failure was NOT a false alarm

An earlier note here claimed the node:fs/node:path errors were a race with
concurrent pnpm installs. That was wrong, and the P3 integrator disproved it.
apps/mobile declares TypeScript ~6.0.3 while the monorepo is on 5.x, so
`pnpm run typecheck` resolves 6.0.3 and fails DETERMINISTICALLY, whereas a bare
`npx tsc` resolves the hoisted 5.9.3 and passes. The two commands were running
different compilers. Fixed with types: ["node"] in 8b114c6; the version skew
itself is still there and is real debt. Lesson: when two ways of running the
same check disagree, find out which binary each resolved before calling either
one a flake.

## Limit and resume mechanics

Limits reset on a rolling window; the error always names the reset time. There
is no quota API, so nothing can poll for it, and an agent that tried would burn
the budget it was watching. The working pattern is:

1. On a limit error, read the reset time from the error text.
2. Update this file's Current position so a cold session loses nothing.
3. `claude-schedule ~/dev/atlitos HH:MM` (a few minutes after reset).
4. launchd fires `claude-resume`, which runs `claude -p` with the PROMPT above.

Observed resets this build: 16:00, 22:20, 01:00 IST, consistent with a rolling
5 hour window that starts on first use after a gap. Two projects running
concurrently share one quota, which is why it drains faster than expected.
