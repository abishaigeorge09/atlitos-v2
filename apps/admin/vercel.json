{
  "buildCommand": "pnpm gen-tokens && tsc --noEmit && vite build",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && pnpm install --filter=@atlitos/admin",
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/index.html" }]
}
