# Founder live-review feedback (Phase 9)

Founder reviews the running app and calls out issues here; the main session logs them so they survive and get batched into a fix pass. Kept SEPARATE from BUG-LEDGER.md (the emulator test session owns that file) to avoid concurrent-write clobbering. Merge these into the ledger + fix in a batch.

| id | surface | founder feedback | type | status | fix direction |
|----|---------|------------------|------|--------|---------------|
| FB-001 | mobile / "You" tab | The "You" tab should show the PROFILE, not settings directly. Settings should be nested inside the profile, reachable from it, not be the whole tab. | UI/IA choice correction (agent chose You=settings; founder wants You=profile) | VERIFIED on-device | Point `apps/mobile/src/app/(tabs)/you.tsx` at the Profile screen content (reuse `profile/index.tsx`) instead of rendering `SettingsContent` directly; keep the Settings entry on the profile header (already added in WS-personalization) + Trainings gear. Check the header avatar->profile route isn't now redundant with the tab. |
| FB-002 | mobile / theme (system) | Under System theme, screens render inconsistently, some white some black. Dark theme not applied uniformly across all screens. | Theme bug (WS-personalization theme switch not wired through every screen) | fixed (phase-10/fix-fb002-dark; card tiles now dark, promo residual is a seed asset) | Root cause of the residual bright tiles: nativewind's native runtime re-resolves single-nested color tokens (`text-text`, `bg-surface-muted` -> `hsl(var(--color-X))`) on the dark toggle, which is why chrome/text/chips go dark, but it does NOT re-resolve the double-nested shadcn compat slots (`bg-card` -> `var(--card)` -> `hsl(var(--color-card))`, and `border-border` -> `var(--border)`), so those stayed pinned at the light `#FFFFFF`/cream `:root` value in dark mode. `product-card.tsx` and `court-card.tsx` (the Home Shop/Recently viewed product tiles and Courts court cards) drew their card surface + border from `bg-card`/`border-border`, so the whole card read bright white in dark. Fix: source the card surface (`colors.card`), border (`colors.border`) and the wishlist heart (`colors.surface`) from the JS `useThemeColors()` path the chrome already uses. Promo carousel already resolves its background/scrim/dots/CTA via JS; its remaining lightness is the uploaded banner artwork (`promo/banner-gear.png`, a light-cream image), a seed asset matter, not a component token, so it needs a dark-friendly banner asset, not a code change. |
| FB-003 | mobile / profile | The profile text should be centered. | UI alignment | VERIFIED on-device | Center the profile name/header text on the profile screen (`apps/mobile/src/app/profile/index.tsx`). Ties in with FB-001 (profile becomes the You tab). |
| FB-004 | mobile / PROFILE own-clip view (NOT the clutch feed) | Clutch FEED is FINE per founder. The problem: viewing your OWN video FROM YOUR PROFILE, the format is wrong + cannot view. Want the profile clip view to look like Instagram. Ref: founder screenshot ~/Desktop/Screenshot 2026-07-29 at 12.26.23 AM.png (IG post: back nav + "Post" title, header row avatar+username+audio track, full-bleed vertical video, mute toggle bottom-right, action bar). | Playback bug + redesign, PROFILE scope | fixed (playback LIVE edge-fn v3, viewer reskinned; tap-verify) | Scope = the profile's own-clip viewer, not the feed. (a) Fix own-clip playback from profile (bad playback URL / owner cannot view own non-published status / stuck processing). (b) Give the profile clip view the IG layout: full-bleed 9:16 video, top header avatar+username+audio, action rail, mute toggle. Find the profile->clip route (profile/index.tsx clip grid -> clip detail). Clutch FEED view is confirmed acceptable, do not change it. |
| FB-005 | mobile / coach search + academy model | Coach-search logic makes no sense: there is no search tab, yet it opens its own tab. Need to rethink around ACADEMY ENROLLMENT: how does discovery/search work for a player enrolled in an academy vs browsing coaches freely? | IA / product design (needs a proposal, not just a fix) | open | Design pass: define the academy-enrollment model and how coach discovery works within it vs open browse; reconcile the home search bar (navigates to /home/search, its own screen) with coach discovery in the coaching/trainings tab. Draft a proposal before coding. |

## Design items needing a proposal (not quick fixes)
- FB-005 academy-enrollment + coach-discovery IA. Founder: "think about how if someone is enrolled in an academy how would that logic work." Draft a short proposal doc, then review with founder before building.
