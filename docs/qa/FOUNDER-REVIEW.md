# Founder live-review feedback (Phase 9)

Founder reviews the running app and calls out issues here; the main session logs them so they survive and get batched into a fix pass. Kept SEPARATE from BUG-LEDGER.md (the emulator test session owns that file) to avoid concurrent-write clobbering. Merge these into the ledger + fix in a batch.

| id | surface | founder feedback | type | status | fix direction |
|----|---------|------------------|------|--------|---------------|
| FB-001 | mobile / "You" tab | The "You" tab should show the PROFILE, not settings directly. Settings should be nested inside the profile, reachable from it, not be the whole tab. | UI/IA choice correction (agent chose You=settings; founder wants You=profile) | fixed | `(tabs)/you.tsx` now renders `<ProfileScreen asTab />` (the same `profile/index.tsx` content), so tapping "You" lands on the profile. Added an `asTab` prop to `ProfileScreen` that swaps the pushed backTitle AppBar for a plain centered "Profile" title (a tab root has no back stack). Settings stays one tap deeper via the existing Settings button on the profile header (and the Trainings gear). The Home header avatar (`(tabs)/index.tsx`) now switches to the You tab instead of pushing a duplicate `/profile` screen; the `/profile` pushed route is kept for the Trainings MySportsCard entry (with its back chevron). phase-9/fixes-ui |
| FB-002 | mobile / theme (system) | Under System theme, screens render inconsistently, some white some black. Dark theme not applied uniformly across all screens. | Theme bug (WS-personalization theme switch not wired through every screen) | fixed | Root cause per the dark-theme sweep: chrome resolves via `useThemeColors()` (JS) and was correct; the three offending light media placeholders used the `bg-surface-muted` className (CSS-var path) which did not follow the theme on native. Swapped all three to the JS-resolved `colors.surfaceMuted` token (same pattern as the profile cover): Home promo banner (`PromoCarousel.tsx`), Home product thumbnails (`ui/product-card.tsx`), Courts court-card image placeholder (`ui/court-card.tsx`). They now render dark in dark mode. Tokens only, no hardcoded hex. phase-9/fixes-ui |
| FB-003 | mobile / profile | The profile text should be centered. | UI alignment | fixed | Centered the profile name/handle/bio block (`alignItems: 'center'` + `textAlign: 'center'`) and the Following/Followers stats row (`justifyContent: 'center'`) in `profile/index.tsx`. Ties in with FB-001 (profile is now the You tab). phase-9/fixes-ui |
| FB-004 | mobile / PROFILE own-clip view (NOT the clutch feed) | Clutch FEED is FINE per founder. The problem: viewing your OWN video FROM YOUR PROFILE, the format is wrong + cannot view. Want the profile clip view to look like Instagram. Ref: founder screenshot ~/Desktop/Screenshot 2026-07-29 at 12.26.23 AM.png (IG post: back nav + "Post" title, header row avatar+username+audio track, full-bleed vertical video, mute toggle bottom-right, action bar). | Playback bug + redesign, PROFILE scope | fixed | Both halves done on `phase-9/fixes-clip`. (a) PLAYBACK: `get-clip-playback-url` widened so the clip OWNER mints a signed URL in ANY status, terminal included (was non-terminal only), so an owner opening their own rejected/pending clip from the profile plays instead of silently falling back to the poster; non-owners still get published only, and admins still cannot mint a removed/rejected clip (takedown teeth intact, scoped strictly to owner-of-row). SQL-proven non-vacuously on the live project (two distinct ids asserted first; owner sees own processing+rejected, non-owner sees neither but sees a published control; decision truth-table over all three statuses). NEEDS EDGE FN REDEPLOY to take effect; RLS unchanged (`clips_select_own` already covered owner-any-status). (b) IG REDESIGN: `(tabs)/clutch/post/[id].tsx` reskinned to Instagram Reels: full-bleed 9:16 `ClipVideo` (was letterboxed maxHeight 420), top bar (back + "Post"), header row (avatar + username + sport/time line), right action rail (like/comment/share), and a mute/unmute toggle (bottom-right); comments moved into a bottom-sheet Modal off the comment action instead of a list pushing the video up. `ClipVideo` gained a controlled `muted` prop (defaults true so the feed is untouched). Feed scroller (`clutch/index.tsx`) unchanged. Docs: VIDEO.md + RLS.md updated. |
| FB-005 | mobile / coach search + academy model | Coach-search logic makes no sense: there is no search tab, yet it opens its own tab. Need to rethink around ACADEMY ENROLLMENT: how does discovery/search work for a player enrolled in an academy vs browsing coaches freely? | IA / product design (needs a proposal, not just a fix) | open | Design pass: define the academy-enrollment model and how coach discovery works within it vs open browse; reconcile the home search bar (navigates to /home/search, its own screen) with coach discovery in the coaching/trainings tab. Draft a proposal before coding. |

## Design items needing a proposal (not quick fixes)
- FB-005 academy-enrollment + coach-discovery IA. Founder: "think about how if someone is enrolled in an academy how would that logic work." Draft a short proposal doc, then review with founder before building.
